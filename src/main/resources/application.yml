spring:
  sql:
    init:
      mode: never  # always / embedded / never
      data-locations: classpath:data.sql
      schema-locations: classpath:schema.sql

  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: ${MYSQL_ROOT_PASSWORD:12345678}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create / update / validate / none
  application:
    name: payment-demo

  # 클라이언트 API 계약 설정 파일 import
  config:
    import: optional:classpath:client-api-config.yml

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    user:
      name: demo
      password: demo123

server:
  port: 8080
  servlet:
    context-path: /

# PortOne Configuration
portone:
  api:
    base-url: https://api.portone.io
    secret: ${PORTONE_API_SECRET:}
  store:
    id: ${PORTONE_STORE_ID:}
  channel:
    kg-inicis: ${PORTONE_CHANNEL_KG:}
    toss: ${PORTONE_CHANNEL_TOSS:your-toss-channel-key}
  webhook:
    secret: ${PORTONE_WEBHOOK_SECRET:UE9SVE9ORV9XRUJIT09LX1NFQ1JFVFBPUlRPTkVfV0VCSE9PS19TRUNSRVRQT1JUT05FX1dFQkhPT0tfU0VDUkVUUE9SVE9ORV9XRUJIT09LX1NFQ1JFVA==}
# JWT Configuration
jwt:
  secret: ${JWT_SECRET:commercehub-secret-key-for-demo-please-change-this-in-production-environment}
  token-validity-in-seconds: ${JWT_VALIDITY:86400}  # 24 hours


# UI Branding Configuration
app:
  ui:
    branding:
      app-name: CommerceHub
      tagline: 통합 커머스 플랫폼
      logo-text: 🏪 CommerceHub

logging:
  level:
    com.bootcamp.paymentdemo: DEBUG
    org.springframework.security: INFO