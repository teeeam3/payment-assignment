spring:
  sql:
    init:
      mode: never   # always / embedded / never
      data-locations: classpath:data.sql
      schema-locations: classpath:schema.sql

  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: ${MYSQL_ROOT_PASSWORD:12345678}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create   # create / update / validate / none
  application:
    name: payment-demo

  # 클라이언트 API 계약 설정 파일 import
  config:
    import: optional:classpath:client-api-config.yml

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    user:
      name: demo
      password: demo123

server:
  port: 8080
  servlet:
    context-path: /

# PortOne Configuration
portone:
  api:
    base-url: https://api.portone.io
    secret: ${PORTONE_API_SECRET:d0njuFO1sblcEtvZ8jIUDkVGEqcZPKrVm5lnRPlyAJfZfdmI5hBhvY1TA9CL37p5Kea41th0nlFmgIZc}
  store:
    id: ${PORTONE_STORE_ID:store-37e7fff3-31a3-4e7a-90e3-2475a7fd1a3f}
  channel:
    kg-inicis: ${PORTONE_CHANNEL_KG:channel-key-fa864ce0-d67f-4b5e-99db-43fba9992d46}
    toss: ${PORTONE_CHANNEL_TOSS:channel-key-24bf3e8c-8bd7-4300-aa1b-8619ce3b4613}
  webhook:
    secret: ${PORTONE_WEBHOOK_SECRET:whsec_hiwD4rWPr46MoMvie8OdoBTOWTc/6tkfzWk7CEDBIPU=}
# JWT Configuration
jwt:
  secret: ${JWT_SECRET:commercehub-secret-key-for-demo-please-change-this-in-production-environment}
  token-validity-in-seconds: ${JWT_VALIDITY:86400}  # 24 hours


# UI Branding Configuration
app:
  ui:
    branding:
      app-name: CommerceHub
      tagline: 통합 커머스 플랫폼
      logo-text: 🏪 CommerceHub

logging:
  level:
    com.bootcamp.paymentdemo: DEBUG
    org.springframework.security: INFO